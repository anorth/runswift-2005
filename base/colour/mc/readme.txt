mc
then load calibration file (ie nnmc.cal)
then load image file (ie *.bfl)

you can manually overwrite the learned calibration (overtraining)
this could be potentially dangerous, so you should run tile 
to check how it affects all the other images.

use save to save the modified calibration file
